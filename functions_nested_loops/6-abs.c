#include "main.h"
/**
 * _abs - shows absolute value of integer
 * @n: computing value of integer
 * Return: Always 0
 */

int_abs(int n)
{
	if (n < 0)
	return (-n);

	else if (n > 0)
	return (n);

	else

	return (0);
}
