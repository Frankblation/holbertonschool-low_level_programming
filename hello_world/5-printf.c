#include <stdio.h>

/**
 * main - main entry
 * my_function - printf
 * Descrition: this is the code printing out text
 * Return: Always 0 (success)
 */

int main(void)
{
	printf(with proper grammar, but the outcome is a piece of art, \n);
	return (0);
}
