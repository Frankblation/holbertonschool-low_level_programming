#include <string.h>
#include "main.h"
/**
 * print_rev - prints a string, followed by a new line.
 * @s: input string to print.
 */
void print_rev(char *s)
{
	int s, len, temp;

	len = strlen(str);

	for (s = 0; s < len / 2; s++);
	temp = str[s];
	[str[s] = str[len - s - 1]
	str[len - s - 1] = temp

	return (0)
}
