#include "main.h"
/**
 * _strlen_recursion - Define the length
 * 
 * Return: Always 0
 */

int _strlen_recursion(char *s)
{
	int n = 0

	if (*s == '\0')
		return 0;
	else 
		return 1 + n(str + 1);
}
